# 97cc65d — The overlay's window is shaped like its pill

## Changes
- `OverlayController`: `pillRegion()` (the capsule) and `applyMask()`, applied on every resize and show of the fallback (non-layer-shell) window; `applyBlurBehind()` reuses the region.

## Additions
- None.

## Bug Fixes
- The HUD pill had a rectangular DWM shadow around it on Windows 11.

## Removals
- None.
