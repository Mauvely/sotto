# aa343d1

**Changes**

- `cmake/MauvelyAppInfo.cmake` — `_linux_formats` is `"appimage"` only;
  the `configure_file()` that generated a Flatpak manifest is removed.
- `.github/workflows/release.yml` — the Linux job is `Linux (AppImage)`;
  no Flatpak install, publish call, or output.
- `.github/workflows/ci.yml` — the comment explaining why Flatpak is
  skipped there is corrected (it no longer builds anywhere).
- `scripts/package.sh` — the dead `flatpak` CLI option and build branch
  removed; `--format all` now only ever means AppImage on Linux.

**Additions**

None.

**Bug Fixes**

Every Linux release was failing to build a Flatpak
(`bwrap: cannot open /proc/sys/user/max_user_namespaces`) and taking the
AppImage down with it in the same step. Removing Flatpak fixes that by
removing the thing that was failing.

**Removals**

- `packaging/linux/build-flatpak.sh`
- `packaging/linux/flatpak/app.yml.in`
